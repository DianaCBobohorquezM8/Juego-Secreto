
/*function calcularIMC(altura,peso){
    var imc = peso / (altura * altura);
      return imc;
}

function calcularFactorial(numero) {
    if (numero === 0 || numero === 1) {
      return 1;
    } else {
      return numero * calcularFactorial(numero - 1);
    }
  }
  // Ejemplo de uso
  let numero = 5;
  let resultado = calcularFactorial(numero);
  console.log(`El factorial de ${numero} es ${resultado}`);

  function convertirDolaresAReales(dolares) {
    var cotizacionDolar = 4.80;
    var reales = dolares * cotizacionDolar;
    return reales;
  }
  // Ejemplo de uso
  let valorEnDolar = 50;
  let valorEnReales = convertirDolaresAReales(valorEnDolar);
  console.log(`${valorEnDolar} dólares  es R$ ${valorEnReales}`);

  function calcularAreaYPerimetroRectangular(altura, anchura) {
    var area = altura * anchura;
    var perimetro = 2 * (altura + anchura);
    console.log("Área: " + area);
    console.log("Perímetro: " + perimetro);
  }
  // Ejemplo de uso
  let altura = 3; // en metros
  let anchura = 5; // en metros
  calcularAreaYPerimetroRectangular(altura, anchura);

  function calcularAreaYPerimetroCircular(radio) {
    var pi = 3.14;
    var area = pi * radio * radio;
    var perimetro = 2 * pi * radio;
    console.log("Área: " + area);
    console.log("Perímetro: " + perimetro);
  }
  // Ejemplo de uso
  let radio = 4; // en metros
  calcularAreaYPerimetroCircular(radio);

  function mostrarTablaDeMultiplicar(numerom) {
    for (var i = 1; i <= 10; i++) {
      var resultado = numerom * i;
      console.log(numerom + " x " + i + " = " + resultado);
    }
  }
  // Ejemplo de uso
  let numerom = 7;
  mostrarTablaDeMultiplicar(numerom);

// function mostrarHola() {
//     console.log("Hola Mundo");
// }
// mostrarHola();

// function recibeParametroNombre(nombre){
//     console.log("Hola Hermoso Perro " + nombre);
// }
// recibeParametroNombre("Arthur");

// function calcularDoble(numero){
//     return numero*2;
// }
// let resultado = calcularDoble(2);
// console.log(resultado);

// function recibeTresNumeros(a,b,c){
//     return (a + b + c) / 3;
// }
// let promedio =recibeTresNumeros(3,6,9);
// console.log(promedio);

// function encontrarMayor(a, b){
//     return a > b ? a : b;
// }
// let numeroMayor = encontrarMayor(4, 9);
// console.log(numeroMayor);

// function cuadrado(numero) {
//     return numero * numero;
// }
// let resultados = cuadrado(8);
// console.log(resultados);
*/
